<configuration>
    <springProperty name="serviceName" source="spring.application.name"/>
    <springProperty name="activeProfile" source="spring.profiles.active"/>

    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
            <providers>
                <timestamp>
                    <fieldName>timestamp</fieldName>
                </timestamp>

                <!-- include service and env together with basic fields -->
                <pattern>
                    <pattern>
                        {
                        "service":"${serviceName:-product-catalog}",
                        "env":"${activeProfile:-default}",
                        "level": "%level",
                        "thread": "%thread",
                        "logger": "%logger{36}"
                        }
                    </pattern>
                </pattern>

                <!-- include all MDC keys (trace ids, request ids, etc.) -->
                <mdc />

                <message>
                    <fieldName>message</fieldName>
                </message>
                <stackTrace>
                    <fieldName>stacktrace</fieldName>
                </stackTrace>

                <!-- include caller information (class/method/line) when available -->
                <callerData>
                    <fieldName>caller</fieldName>
                </callerData>
            </providers>
        </encoder>

    </appender>

    <root level="INFO">
        <appender-ref ref="STDOUT" />
    </root>
</configuration>
